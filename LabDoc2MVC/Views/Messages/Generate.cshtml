@using DLL.Models
@model DLL.Models.Message[]?

@{
    ViewBag.Title = "Messages";
    Layout = "_Layout";
}

<h2>Generated messages</h2>

<table class="table-bordered">
    <thead>
    <tr>
        <th>Text</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var message in @Model ?? Enumerable.Empty<Message>())
    {
        <tr>
            <td>@message.Text</td>
        </tr>
    }
    </tbody>
</table>